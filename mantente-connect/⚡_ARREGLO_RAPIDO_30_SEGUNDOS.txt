╔════════════════════════════════════════════════════════════════╗
║  ⚡ ARREGLO RÁPIDO EN 30 SEGUNDOS                              ║
║     (Loop Infinito de Sincronización)                           ║
╚════════════════════════════════════════════════════════════════╝

🎯 PROBLEMA:
  • Pantalla parpadea constantemente
  • Mensaje "Sincronizando..." no desaparece
  • Error 400 en la consola

✅ SOLUCIÓN APLICADA:
  1. SyncManager.jsx → Debounce de 5 segundos entre reintentos
  2. dataStore.js → NO recarga datos si hay errores en sincronización

════════════════════════════════════════════════════════════════

📋 QUÉ HACER AHORA (3 pasos):

  1. REINICIA LA APP:
     Terminal → Ctrl + C
     Terminal → npm run dev

  2. LIMPIA CACHE:
     Navegador → Ctrl + Shift + R

  3. ABRE CONSOLA:
     Navegador → F12
     Ve a Console tab

════════════════════════════════════════════════════════════════

✅ ESPERADO DESPUÉS:
  ✓ Pantalla deja de parpadear
  ✓ Ves sincronización con reintentos (1/3, 2/3, 3/3)
  ✓ Se respeta espera de 5 segundos entre intentos
  ✓ Después de 3 intentos, pausa 30 segundos

❌ SI SIGUE PARPADEANDO:
  → Error 400 persiste
  → Abre DevTools → Network tab
  → Busca solicitud fallida (400)
  → Copia el error exacto

════════════════════════════════════════════════════════════════

🆘 EMERGENCIA - Limpiar cambios atrapados:
  F12 → Console → Pega esto:
  
  (async () => {
    const { openDB } = await import('https://cdn.jsdelivr.net/npm/idb@8.0.0/+esm');
    const db = await openDB('mantente-db');
    const tx = db.transaction('sync_queue', 'readwrite');
    await tx.store.clear();
    console.log('✅ Limpiada');
  })();
  
  Luego: F5 (recarga página)

════════════════════════════════════════════════════════════════

📞 PRÓXIMO PASO:
  1. Actualiza la app (npm run dev)
  2. Refresca navegador (Ctrl + Shift + R)
  3. Observa consola (F12)
  4. Cuéntame si funciona

⏱️ Tiempo estimado: 30 segundos