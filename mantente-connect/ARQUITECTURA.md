# 🏗️ ARQUITECTURA - MANTENTE CONNECT

## Sistema de Capas

```
┌─────────────────────────────────────────────────────┐
│              CAPA DE PRESENTACIÓN                   │
│  ┌──────────────────────────────────────────────┐   │
│  │         React Components (JSX)               │   │
│  │  ┌──────────────┐  ┌──────────────────────┐ │   │
│  │  │  Navbar      │  │  Pages (6 rutas)    │ │   │
│  │  ├──────────────┤  ├──────────────────────┤ │   │
│  │  │  Footer      │  │  - Login             │ │   │
│  │  │  Components  │  │  - Dashboard         │ │   │
│  │  │  (Cards,     │  │  - Inventory         │ │   │
│  │  │   Forms)     │  │  - Customers         │ │   │
│  │  │              │  │  - Orders            │ │   │
│  │  │              │  │  - Settings          │ │   │
│  │  └──────────────┘  └──────────────────────┘ │   │
│  └──────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
                         ↕ (Props, State)
┌─────────────────────────────────────────────────────┐
│         CAPA DE ENRUTAMIENTO                        │
│       React Router v6                               │
│   ┌──────────────────────────────────────────────┐  │
│   │  ProtectedRoutes (requiere autenticación)    │  │
│   │  PublicRoutes (login, etc)                   │  │
│   └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
                         ↕
┌─────────────────────────────────────────────────────┐
│       CAPA DE ESTADO GLOBAL (Zustand)              │
│  ┌──────────────────────────────────────────────┐   │
│  │          useAuthStore                        │   │
│  │  - user, session, isLoading, error           │   │
│  │  - login(), logout(), restoreSession()       │   │
│  └──────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────┐   │
│  │       useInventoryStore                      │   │
│  │  - products[], syncStatus                    │   │
│  │  - addProduct(), updateProduct()             │   │
│  │  - getUnsyncedProducts()                     │   │
│  └──────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────┐   │
│  │       useCustomersStore                      │   │
│  │  - customers[], syncStatus                   │   │
│  │  - addCustomer(), updateCustomer()           │   │
│  │  - getUnsyncedCustomers()                    │   │
│  └──────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────┐   │
│  │        useOrdersStore                        │   │
│  │  - orders[], syncStatus                      │   │
│  │  - addOrder(), updateOrder()                 │   │
│  │  - getOrdersByCustomer()                     │   │
│  └──────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
                         ↕
┌─────────────────────────────────────────────────────┐
│       CAPA DE SERVICIOS                             │
│  ┌──────────────────────────────────────────────┐   │
│  │    databaseService (IndexedDB/Dexie)         │   │
│  │  - CRUD local: productos, clientes, órdenes  │   │
│  │  - Gestión de sesiones                       │   │
│  │  - Cola de sincronización                    │   │
│  └──────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────┐   │
│  │      syncService (Sincronización)             │   │
│  │  - Detector de conectividad (online/offline) │   │
│  │  - Comparación de timestamps                 │   │
│  │  - Cola de cambios pendientes                │   │
│  │  - Resolución de conflictos                  │   │
│  └──────────────────────────────────────────────┘   │
│  ┌──────────────────────────────────────────────┐   │
│  │    initializeService (Inicialización)        │   │
│  │  - Restaurar sesión                          │   │
│  │  - Cargar datos locales                      │   │
│  │  - Configurar listeners                      │   │
│  └──────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
                         ↕
┌─────────────────────────────────────────────────────┐
│       CAPA DE ALMACENAMIENTO LOCAL                  │
│  ┌──────────────────────────────────────────────┐   │
│  │        IndexedDB (Dexie.js)                  │   │
│  │                                              │   │
│  │  ┌────────────────────────────────────────┐  │   │
│  │  │  ManteneConnectDB                      │  │   │
│  │  ├────────────────────────────────────────┤  │   │
│  │  │  Tablas:                               │  │   │
│  │  │  • products (id, name, sku, synced)    │  │   │
│  │  │  • customers (id, name, email, synced) │  │   │
│  │  │  • orders (id, customerId, status)     │  │   │
│  │  │  • orderItems (id, orderId, productId) │  │   │
│  │  │  • syncQueue (cambios pendientes)      │  │   │
│  │  │  • sessions (tokens cacheados)         │  │   │
│  │  └────────────────────────────────────────┘  │   │
│  │                                              │   │
│  │  localStorage - Datos simples               │   │
│  └──────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
                         ↕ (HTTP/REST)
┌─────────────────────────────────────────────────────┐
│       BACKEND REMOTO (Supabase)                     │
│  ┌──────────────────────────────────────────────┐   │
│  │  PostgreSQL Database                         │   │
│  │  • products                                  │   │
│  │  • customers                                 │   │
│  │  • orders                                    │   │
│  │                                              │   │
│  │  Supabase Auth                               │   │
│  │  • JWT tokens                                │   │
│  │  • User management                           │   │
│  │                                              │   │
│  │  Storage                                     │   │
│  │  • Imágenes, documentos                      │   │
│  └──────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

---

## Flujo de Datos

### 📝 Crear Producto (Offline)

```
Usuario escribe en formulario
        ↓
Component: InventoryPage.jsx
        ↓
useInventoryStore.addProduct()
        ↓
Zustand → guarda en state
        ↓
dbService.addProduct()
        ↓
IndexedDB (local)
        ↓
✅ Producto guardado LOCALMENTE
✅ UI actualizada instantáneamente
⏳ En cola para sincronizar cuando haya red
```

### 🔄 Sincronización (Online)

```
App detecta conectividad
        ↓
setupOnlineListener() → handleOnline()
        ↓
syncService.syncData()
        ↓
Obtiene cola de cambios:
dbService.getSyncQueue()
        ↓
Por cada cambio:
  - Valida timestamp
  - Compara con servidor
  - Resuelve conflictos
        ↓
Envía a Supabase:
supabaseClient.from('products').upsert()
        ↓
Si éxito:
  - Actualiza synced=true
  - Limpia cola
  - Actualiza lastSync
        ↓
✅ Todo sincronizado
```

### 👤 Autenticación

```
Usuario hace login
        ↓
LoginPage.jsx → useAuthStore.login()
        ↓
Llamada a:
supabaseClient.auth.signInWithPassword()
        ↓
Si éxito:
  - Guarda user
  - Guarda session
  - Persiste en localStorage
  - Redirige a dashboard
        ↓
Si error:
  - Muestra error
  - Limpia formulario
        ↓
En app restart:
  - useAuthStore restaura desde localStorage
  - Permite funcionar offline con sesión cacheada
```

---

## Estados de Sincronización

```
┌─────────────┐
│    IDLE     │ (esperando)
└──────┬──────┘
       │ (cambios detectados + online)
       ↓
┌─────────────┐
│   SYNCING   │ (sincronizando)
└──────┬──────┘
       │
  ┌────┴────┐
  ↓         ↓
SUCCESS   ERROR
  │         │
  └────┬────┘
       ↓
   IDLE (vuelve a esperar)
```

---

## Flujo de Datos en Componente

```javascript
import React, { useEffect } from 'react'
import { useInventoryStore } from '@/store/inventoryStore'

export default function InventoryPage() {
  // 1. Conectar con el store
  const { products, addProduct, updateProduct } = useInventoryStore()

  // 2. Efecto al montar
  useEffect(() => {
    // Cargar datos locales si no están
    if (products.length === 0) {
      // Obtener del store (ya están en IndexedDB)
    }
  }, [])

  // 3. Manejar cambios
  const handleAddProduct = (data) => {
    // Esto automáticamente:
    // - Actualiza Zustand
    // - Guarda en IndexedDB
    // - Marca como no sincronizado
    addProduct(data)
  }

  // 4. Renderizar
  return (
    <div>
      {products.map(p => (
        <ProductCard key={p.id} product={p} />
      ))}
    </div>
  )
}
```

---

## Persistencia de Datos

### LocalStorage
- Sesión del usuario
- Configuraciones
- Tokens JWT

### IndexedDB (Dexie)
- Productos
- Clientes
- Órdenes
- Historial de cambios
- Cola de sincronización

---

## Seguridad

```
┌──────────────────────────────────────┐
│    Usuario intenta acceder            │
└──────────────────────────────────────┘
           ↓
┌──────────────────────────────────────┐
│  ¿Hay JWT en localStorage?            │
└──────────────────────────────────────┘
      ↙          ↘
    Sí            No
    ↓             ↓
 LOGIN      OFFLINE
(si está    (usar caché
 online)    si existe)
    │             │
    └──────┬──────┘
           ↓
┌──────────────────────────────────────┐
│  ¿Es válido el JWT?                  │
│  ¿No expiró?                         │
└──────────────────────────────────────┘
      ↙          ↘
    Sí            No
    ↓             ↓
 ACCESO      LOGOUT
 PERMITIDO   REQUERIDO
```

---

## Ventajas de Esta Arquitectura

✅ **Offline-First**: Funciona sin internet
✅ **Sincronización Automática**: Se sincroniza cuando se puede
✅ **Escalable**: Fácil agregar nuevas funciones
✅ **Performante**: IndexedDB es rápido
✅ **Flexible**: Compatible con web, desktop, móvil
✅ **Segura**: Validación en ambos lados
✅ **Separación de Responsabilidades**: Cada capa tiene su rol

---

## Próximas Mejoras

- 🔐 Encriptación de datos locales
- 🔄 Sincronización bidireccional más robusta
- 📱 Soporte para PWA
- 🎯 Compresión de datos
- 📊 Analytics de sincronización
- 🚀 Service Workers para caché
